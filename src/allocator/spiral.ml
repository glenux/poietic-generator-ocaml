
type vector = {
  x: int ;
  y: int
}

let initialize x y = begin
    ()
end
 
let rotate_left { x ; y } = begin
    { x = b ; y = -a }
end

let rotate_right { a ; b } = begin
    { x= -b, y = a }
end
